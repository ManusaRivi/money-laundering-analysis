worker:
  type: "Monitor"
  params:
    bully:
      tcp_host: "0.0.0.0"
      tcp_port: 9001
      ping_interval: "1.5s"
      ping_timeout: "500ms"
    monitoring:
      udp_port: 9000
      ping_interval: "5s"
      ping_timeout: "1s"
      failure_threshold: 3
