broker:
  type: q-q
  input:
    queue:
      name: q_q5_count
  output:
    queue:
      name: query_results

worker:
  type: "Aggregator"
  params:
    type: count
  query: 5
  sync_eof:
    retries_base_delay: 0.5
    retries_step_delay: 0.5
    max_retries: 5
